# DESACTIVADO: este workflow subía el repo crudo a Pages y rompía el sitio Astro.
# El despliegue correcto está en .github/workflows/astro.yml (build + dist).
# Se deja el archivo vacío de triggers para no competir por el entorno "pages".
name: Deploy static content to Pages (disabled)

on:
  workflow_dispatch:
    # Solo manual, y no debería usarse. Preferir "Portafolio Misael" (astro.yml).

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow desactivado. Usa astro.yml"
